import React from 'react';
import { shallow } from 'enzyme';

import NewArrivals from './new-arrivals.container';

describe('NewArrivals', () => {
    it('Test example', () => {
        // const wrapper = shallow(
        //     <NewArrivals />
        // );
        expect(true).toBe(true);
    });
});