import { GET_API, POST_API, DELETE_API, PUT_API } from "../actions/actions";

export const products = (state = [], action) => {

  switch (action.type) {

    case 'demo':
     return state

    default:
      return state

  }

}